import React from 'react'

const Exchanges = () => {
	return (
		<div>
			Thank you for reaching this page. Unfortunately, the API required for this now requires a paid membership and therefore, this
      content will no longer be suported :(
		</div>
	)
}

export default Exchanges
